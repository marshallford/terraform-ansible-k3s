[project]
name = "terraform-ansible-k3s"
version = "0.1.0"
requires-python = ">=3.14.5"
dependencies = [
    "ansible>=13.7.0",
    "ansible-builder>=3.1.1",
    "ansible-lint>=26.4.0",
]
[tool.uv]
required-version = ">=0.11.15"
