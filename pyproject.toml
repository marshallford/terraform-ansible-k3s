[project]
name = "terraform-ansible-k3s"
version = "0.1.0"
requires-python = ">=3.14.7"
dependencies = [
    "ansible>=14.3.1",
    "ansible-builder>=3.1.1",
    "ansible-lint>=26.8.0",
]
[tool.uv]
required-version = ">=0.12.5"
