ansible-navigator:
  execution-environment:
    container-engine: docker
    image: ghcr.io/marshallford/terraform-ansible-k3s:ci
    pull:
      policy: never
  logging:
    file: /dev/null
  mode: stdout
  playbook-artifact:
    enable: false
